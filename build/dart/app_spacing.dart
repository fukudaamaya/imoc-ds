// AUTO-GENERATED by Style Dictionary from tokens/primitives.json, tokens/color.json, tokens/dimensions.json.
// Do not edit this file directly — edit variables in Figma, run `npm run sync-figma`, then `npm run tokens:build`.
// Dimension values below use the Mobile mode from the Dimensions collection (this file feeds the Flutter client).

/// Spacing, radius, and layout constants — Mobile-mode values from the Dimensions collection.
class AppSpacingScale {
  AppSpacingScale._();

  /// Space 100 (4px). Fine-adjustment step. Aliased by spacing/micro-gap.
  static const double scale100 = 4;

  /// Space 200 (8px). Aliased by spacing/inline-gap. Also the minimum separation between adjacent touch targets.
  static const double scale200 = 8;

  /// Space 300 (12px). Fine-adjustment step. Aliased by spacing/content-gap and spacing/component-padding on mobile.
  static const double scale300 = 12;

  /// Space 400 (16px). Aliased by spacing/page-margin, card-padding, content-gap, stack-gap, grid-gap on mobile.
  static const double scale400 = 16;

  /// Space 500 (24px). Aliased by spacing/card-padding, card-padding-loose, grid-gap on web.
  static const double scale500 = 24;

  /// Space 600 (32px). Aliased by spacing/page-margin and block-gap on web, card-padding-loose on web.
  static const double scale600 = 32;

  /// Space 700 (40px). Aliased by spacing/block-gap on web.
  static const double scale700 = 40;

  /// Space 800 (48px). Aliased by spacing/section-gap on mobile. Matches the platform constraint for mobile section padding.
  static const double scale800 = 48;

  /// Space 900 (56px). Unaliased — available between section-gap steps.
  static const double scale900 = 56;

  /// Space 1000 (64px). Aliased by spacing/section-gap-loose on mobile.
  static const double scale1000 = 64;

  /// Space 1100 (80px). Aliased by spacing/section-gap on web. Matches the platform constraint for desktop section padding.
  static const double scale1100 = 80;

  /// Space 1200 (96px). Aliased by spacing/section-gap-loose on web — signature moments only.
  static const double scale1200 = 96;
}

class AppSpacing {
  AppSpacing._();

  /// Left and right page gutters on every route. Matches the grid margin — always use this, never a raw value. 16 mobile / 32 web.
  static const double pageMargin = 16;

  /// Vertical space between major page sections — hero to intro, intro to treatment grid, grid to CTA band. 48 mobile / 80 web.
  static const double sectionGap = 48;

  /// Signature moments only — around the hero, the longevity programme block, the booking CTA. Use sparingly. 64 mobile / 96 web.
  static const double sectionGapLoose = 64;

  /// Between content blocks inside a section — heading group to card grid, paragraph cluster to image. 32 mobile / 40 web.
  static const double blockGap = 32;

  /// Internal padding on treatment cards, condition cards, FAQ panels, practitioner bios. 16 mobile / 24 web.
  static const double cardPadding = 16;

  /// Featured cards, methodology callouts, testimonial panels. 24 mobile / 32 web.
  static const double cardPaddingLoose = 24;

  /// Between related text elements — overline to heading, heading to body, label to input. 12 mobile / 16 web.
  static const double contentGap = 12;

  /// Between repeated items in a vertical list — condition rows, FAQ items, form fields. Fixed at 16: list rhythm should not change by platform.
  static const double stackGap = 16;

  /// Between cards in a grid or horizontal rail. Matches the grid gutter. 16 mobile / 24 web.
  static const double gridGap = 16;

  /// Between elements on one line — icon to label, chip to chip, button to button. Fixed at 8. Also the minimum gap between adjacent touch targets.
  static const double inlineGap = 8;

  /// Internal padding on buttons, inputs, chips, badges, select fields. 12 mobile / 16 web. With type/label this yields a 46px button — clears the 44px minimum.
  static const double componentPadding = 12;

  /// Tightest relationship — badge internals, icon to count, required-field asterisk to label. Fixed at 4.
  static const double microGap = 4;
}

class AppRadiusScale {
  AppRadiusScale._();

  /// Radius 100 (4px). Aliased by radius/small.
  static const double scale100 = 4;

  /// Radius 200 (8px). Aliased by radius/medium — the system default.
  static const double scale200 = 8;

  /// Radius 300 (12px). Aliased by radius/large.
  static const double scale300 = 12;

  /// Radius 400 (24px). Aliased by radius/xl. Note the jump from 12 — 16px was deliberately removed as an unused escape hatch.
  static const double scale400 = 24;

  /// Radius full (9999px). Aliased by radius/pill for fully rounded shapes.
  static const double scaleFull = 9999;
}

class AppRadius {
  AppRadius._();

  /// Checkboxes, small tags, colour swatches, inline code, image thumbnails under 48px. 4px, identical on both platforms.
  static const double small = 4;

  /// THE DEFAULT. Buttons, inputs, selects, textareas, treatment and condition cards, FAQ panels, dropdown menus. 8px — deliberately not a pill, which would read as wellness-franchise rather than physician-led.
  static const double medium = 8;

  /// Modals, mobile bottom sheets, featured cards, methodology callouts, image containers. 12px.
  static const double large = 12;

  /// Full-bleed rounded sections, hero image containers, the longevity programme block. 24px.
  static const double xl = 24;

  /// Filter chips, condition tags, category badges, avatars, toggle switches, progress dots. Fully rounded — only for shapes that are genuinely pill or circular.
  static const double pill = 9999;
}

class AppLayout {
  AppLayout._();

  /// Outer bound for all page content, centred. Beyond 1440 the extra viewport width becomes page margin, not layout. Uncapped on mobile. Wraps every route.
  static const double maxWidthContainer = 9999;

  /// Long-form reading measure — treatment descriptions, condition copy, about text. 512px on web is 73 characters at 16px Satoshi (measured, avg char 6.99px), inside the 65–75ch constraint. Uncapped on mobile. On desktop the remaining container width holds the related-conditions rail, in-page nav, and booking CTA.
  static const double maxWidthText = 9999;

  /// Single-column forms — the intake flow, booking form, newsletter signup, error and empty states. 400px on web keeps forms from sprawling; uncapped on mobile where the viewport is the constraint.
  static const double maxWidthNarrow = 9999;

  /// Fixed site header height. Sticky on both platforms. 64 mobile / 80 web.
  static const double headerHeight = 64;

  /// Mobile persistent booking bar at the viewport bottom. Add this to bottom page padding so the last element is never covered. 0 on web — the bar is mobile-only.
  static const double stickyBarHeight = 64;
}
