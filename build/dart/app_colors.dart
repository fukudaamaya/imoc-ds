// AUTO-GENERATED by Style Dictionary from tokens/primitives.json, tokens/color.json, tokens/dimensions.json.
// Do not edit this file directly — edit variables in Figma, run `npm run sync-figma`, then `npm run tokens:build`.
// Dimension values below use the Mobile mode from the Dimensions collection (this file feeds the Flutter client).

import 'package:flutter/widgets.dart';

/// Primitive color ramps (ocean, earth, flora, neutral, success, warning, error, info).
/// Prefer [AppColors] (semantic) in product code — reach for these only for illustration,
/// data-vis, or building a new semantic token.
class AppColorPrimitives {
  AppColorPrimitives._();

  /// Ocean 50. Lightest brand wash. Aliased by surface/brand-subtle for condition-match banners and selected filter chips.
  static const Color ocean50 = Color(0xFFF0FAFA);

  /// Ocean 100. Light brand tint. Unaliased — reserved for hover states on brand-subtle surfaces.
  static const Color ocean100 = Color(0xFFD6F0F1);

  /// Ocean 200. Absorbs the retired Sky #9BD6CF. Unaliased — available for illustration and decorative fills.
  static const Color ocean200 = Color(0xFFA8DFE1);

  /// Ocean 300. Aliased by text/link-inverse. 9.51:1 on neutral/950 — the only ocean step safe for text on dark surfaces.
  static const Color ocean300 = Color(0xFF6EC7CA);

  /// Ocean 400. Mid brand tone. Unaliased — available for illustration and data visualisation.
  static const Color ocean400 = Color(0xFF2FADB0);

  /// Ocean 500. Unaliased — sits between the mid tones and the brand value.
  static const Color ocean500 = Color(0xFF158F92);

  /// Ocean 600. THE BRAND VALUE from the style tile. Aliased by border/brand. Fails body text on white (4.44:1) — never use for text under 24px.
  static const Color ocean600 = Color(0xFF0D8588);

  /// Ocean 700. The text and action step. Aliased by surface/brand, surface/action, text/link, text/brand, border/focus. 6.29:1 on white.
  static const Color ocean700 = Color(0xFF0A6B6D);

  /// Ocean 800. Aliased by surface/action-hover and text/link-hover. 9.22:1 with white.
  static const Color ocean800 = Color(0xFF075052);

  /// Ocean 900. Aliased by surface/action-pressed for the active state on primary buttons.
  static const Color ocean900 = Color(0xFF04302F);

  /// Earth 50. Lightest warm tint. Unaliased — available as an alternate page wash.
  static const Color earth50 = Color(0xFFFBF8F5);

  /// Earth 100. Aliased by surface/accent-subtle for alternating warm section washes, quote blocks, story sections.
  static const Color earth100 = Color(0xFFF6EEE3);

  /// Earth 200. Unaliased — available for illustration fills and image mattes.
  static const Color earth200 = Color(0xFFEDDCC7);

  /// Earth 300. THE SAND BRAND VALUE from the style tile. Aliased by surface/decorative for illustration and image mattes. Never text (1.61:1 on surface/page).
  static const Color earth300 = Color(0xFFDFC3A4);

  /// Earth 400. Unaliased — illustration and decorative fills only. Fails all text contrast (2.37:1 on white).
  static const Color earth400 = Color(0xFFC9A177);

  /// Earth 500. Unaliased. 3.44:1 on white — large text and UI only, never body copy.
  static const Color earth500 = Color(0xFFB0814F);

  /// Earth 600. Lightest text-safe warm step. Aliased by border/accent for pull-quote rules and treatment card dividers. 5.41:1 on white.
  static const Color earth600 = Color(0xFF8E6039);

  /// Earth 700. THE EARTH BRAND VALUE from the style tile. Aliased by surface/accent and text/accent. 8.83:1 on white.
  static const Color earth700 = Color(0xFF6C3F27);

  /// Earth 800. Unaliased — reserved for deep editorial surfaces. 12.83:1 with white.
  static const Color earth800 = Color(0xFF4A2A1A);

  /// Earth 900. Deepest warm tone. Source hue for the warm-tinted elevation shadows.
  static const Color earth900 = Color(0xFF2A170D);

  /// Flora 50. Lightest botanical tint. Supplement line only — reserved for surface/page or surface/brand-subtle when the Supplement mode is rebuilt. Unaliased in Clinic.
  static const Color flora50 = Color(0xFFF6FBF0);

  /// Flora 100. Supplement line only. Reserved for surface/brand-subtle when the Supplement mode is filled in.
  static const Color flora100 = Color(0xFFEAF6DC);

  /// Flora 200. Light botanical tint. Supplement line only — candidate for surface/selected and hover states in the Supplement theme. Unaliased in Clinic.
  static const Color flora200 = Color(0xFFCFE9B5);

  /// Flora 300. THE FLORA BRAND VALUE from the style tile. RESTRICTED: surface and illustration fills only, never status indicators — it would collide with success green.
  static const Color flora300 = Color(0xFFB2D78B);

  /// Flora 400. Mid botanical tone. Supplement line only — illustration and packaging-adjacent fills. Fails text contrast on white; never use for copy.
  static const Color flora400 = Color(0xFF93C165);

  /// Flora 500. Supplement line only — candidate for border/brand and non-text indicators in the Supplement theme. Verify against 3:1 before committing.
  static const Color flora500 = Color(0xFF74A445);

  /// Flora 600. Supplement line only. Candidate for surface/action in Supplement mode — verify white contrast before use.
  static const Color flora600 = Color(0xFF5A8434);

  /// Flora 700. Supplement line only. Candidate for text/link in Supplement mode. 6.54:1 on flora/50.
  static const Color flora700 = Color(0xFF436327);

  /// Flora 800. Deep botanical tone. Supplement line only — reserved for dark editorial surfaces in the Supplement theme. Unaliased in Clinic.
  static const Color flora800 = Color(0xFF2C421A);

  /// Flora 900. Deepest botanical tone. Supplement line only — reserved for surface/inverse in the Supplement theme if it needs to differ from neutral/950. Unaliased in Clinic.
  static const Color flora900 = Color(0xFF17230E);

  /// Neutral 0. Pure white. Aliased by surface/card, surface/input, surface/elevated, text/on-brand, text/on-accent.
  static const Color neutral0 = Color(0xFFFFFFFF);

  /// Neutral 50. Warm off-white. Aliased by surface/page and text/inverse. Chosen over pure white to reduce glare for light-sensitive readers.
  static const Color neutral50 = Color(0xFFFAFAF8);

  /// Neutral 100. Aliased by surface/disabled and surface/action-disabled for inactive buttons and unavailable appointment slots.
  static const Color neutral100 = Color(0xFFF5F3F0);

  /// Neutral 200. Aliased by border/subtle for list dividers, table row rules, section separators.
  static const Color neutral200 = Color(0xFFE9E6E1);

  /// Neutral 300. Aliased by border/default for card outlines and non-interactive container edges.
  static const Color neutral300 = Color(0xFFD6D2CA);

  /// Neutral 400. Unaliased — available for decorative rules on dark surfaces.
  static const Color neutral400 = Color(0xFFBFBBB1);

  /// Neutral 500. Aliased by text/disabled. 2.54:1 on surface/disabled — an intentional WCAG 1.4.3 exemption for inactive controls.
  static const Color neutral500 = Color(0xFF9E9A90);

  /// Neutral 600. RETUNED from #7D7970 so tertiary text passes AA. Aliased by text/tertiary and border/strong. 4.60:1 on page, 4.81:1 on card.
  static const Color neutral600 = Color(0xFF767266);

  /// Neutral 700. Aliased by text/secondary for card subtitles, treatment duration, metadata, breadcrumbs. 6.57:1 on page.
  static const Color neutral700 = Color(0xFF5E5A52);

  /// Neutral 800. Unaliased — available for a fourth text tier if long treatment pages need one.
  static const Color neutral800 = Color(0xFF3F3C36);

  /// Neutral 900. Aliased by text/primary — the default reading colour for all body copy and headings. 14.78:1 on page.
  static const Color neutral900 = Color(0xFF272420);

  /// Neutral 950. The brand near-black from the style tile. Aliased by surface/inverse for the footer and longevity sections. Also the source of the overlay scrim.
  static const Color neutral950 = Color(0xFF141210);

  /// Success 50. Aliased by surface/success for booking confirmed panels and saved-intake banners.
  static const Color success50 = Color(0xFFEDF7E9);

  /// Success 500. Aliased by border/success. 3.93:1 on page — clears 3:1 for non-text UI, not for body copy.
  static const Color success500 = Color(0xFF3E8E2E);

  /// Success 700. Aliased by text/success. 8.01:1 on surface/success.
  static const Color success700 = Color(0xFF235517);

  /// Warning 50. Aliased by surface/warning for out-of-state logistics notices and limited-availability strips.
  static const Color warning50 = Color(0xFFFDF3E3);

  /// Warning 500. Aliased by border/warning. 3.66:1 on page — non-text UI only.
  static const Color warning500 = Color(0xFFB8730E);

  /// Warning 700. Aliased by text/warning. 7.81:1 on surface/warning.
  static const Color warning700 = Color(0xFF6E4207);

  /// Error 50. Aliased by surface/error for validation summaries and failed submission banners.
  static const Color error50 = Color(0xFFFDECEA);

  /// Error 500. Aliased by border/error for failed input outlines. 5.03:1 on page.
  static const Color error500 = Color(0xFFC8352A);

  /// Error 700. Aliased by text/error. 8.89:1 on surface/error.
  static const Color error700 = Color(0xFF7C1F17);

  /// Info 50. Aliased by surface/info for cash-pay policy notes and what-to-bring callouts.
  static const Color info50 = Color(0xFFE8F1F9);

  /// Info 500. Aliased by border/info. 4.94:1 on page. True blue, deliberately not Ocean, so system state never reads as brand.
  static const Color info500 = Color(0xFF2172AC);

  /// Info 700. Aliased by text/info. 8.94:1 on surface/info.
  static const Color info700 = Color(0xFF144468);
}

/// Semantic color tokens — text, surface, border. These are what product code should use.
class AppColors {
  AppColors._();

  /// Body copy, treatment descriptions, headings, form labels — the default reading colour. 14.78:1 on surface/page, 15.45:1 on surface/card.
  static const Color textPrimary = Color(0xFF272420);

  /// Card subtitles, treatment duration lines, metadata, breadcrumbs, "last updated" stamps. 6.57:1 on surface/page, 6.86:1 on surface/card.
  static const Color textSecondary = Color(0xFF5E5A52);

  /// Helper text under form inputs, footnotes, supporting detail inside cards. 4.60:1 on surface/page, 4.81:1 on surface/card — passes AA body.
  static const Color textTertiary = Color(0xFF767266);

  /// Disabled button labels, unavailable appointment slot text, locked form step labels. 2.54:1 on surface/disabled — intentional WCAG 1.4.3 exemption. Always pair with a non-colour cue.
  static const Color textDisabled = Color(0xFF9E9A90);

  /// Body copy and headings in the footer and dark longevity sections. 17.88:1 on surface/inverse.
  static const Color textInverse = Color(0xFFFAFAF8);

  /// Inline links in body copy, "see related treatments", in-page nav links. 6.02:1 on surface/page, 6.29:1 on surface/card. Always underlined — colour is never the sole cue.
  static const Color textLink = Color(0xFF0A6B6D);

  /// Hover and focus state on inline links. 8.82:1 on surface/page.
  static const Color textLinkHover = Color(0xFF075052);

  /// Links inside the footer and dark longevity sections. 9.51:1 on surface/inverse.
  static const Color textLinkInverse = Color(0xFF6EC7CA);

  /// Ocean-coloured headings, eyebrow labels above section titles, step numerals in the intake flow. 6.02:1 on surface/page.
  static const Color textBrand = Color(0xFF0A6B6D);

  /// Earth-coloured editorial headings, pull quotes, treatment category labels. 8.45:1 on surface/page.
  static const Color textAccent = Color(0xFF6C3F27);

  /// Text and icons on any dark filled surface — primary button labels on surface/action, headings on surface/brand, copy on surface/accent editorial blocks. 6.29:1 on surface/action and surface/brand (ocean/700), 8.83:1 on surface/accent (earth/700). Replaces the former text/on-brand and text/on-accent, which were the same white doing the same job.
  static const Color textOnFill = Color(0xFFFFFFFF);

  /// Confirmation copy — "Your request is in, we’ll call within one business day." 8.01:1 on surface/success.
  static const Color textSuccess = Color(0xFF235517);

  /// Advisory copy — out-of-state travel notes, limited availability messages. 7.81:1 on surface/warning.
  static const Color textWarning = Color(0xFF6E4207);

  /// Validation messages beside the failing field. Supportive tone, never blaming. 8.89:1 on surface/error.
  static const Color textError = Color(0xFF7C1F17);

  /// Explanatory copy — cash-pay policy, what to bring, telehealth eligibility. 8.94:1 on surface/info.
  static const Color textInfo = Color(0xFF144468);

  /// Placeholder text inside empty form fields — "you@example.com", "Search conditions". 4.81:1 on surface/input. Distinct from text/tertiary (helper text below a field) and text/disabled (inactive control).
  static const Color textPlaceholder = Color(0xFF767266);

  /// Base page canvas behind all content on every route. Warm off-white rather than pure white to reduce glare for light-sensitive readers.
  static const Color surfaceBackground = Color(0xFFFAFAF8);

  /// Treatment cards, condition cards, practitioner bio panels, FAQ accordions. Lifts off the page without needing a border.
  static const Color surfaceCard = Color(0xFFFFFFFF);

  /// Text fields, selects, textareas, date pickers in the booking and intake forms. Always pair with border/strong.
  static const Color surfaceInput = Color(0xFFFFFFFF);

  /// Footer, longevity programme sections, testimonial features. The primary aspirational register for longevity-seeking visitors.
  static const Color surfaceInverse = Color(0xFF141210);

  /// Disabled buttons, unavailable appointment slots, locked form steps.
  static const Color surfaceDisabled = Color(0xFFE9E6E1);

  /// Ocean feature panels, condition-finder headers, quiz intro blocks. Pair with text/on-brand — 6.29:1.
  static const Color surfaceBrand = Color(0xFF0A6B6D);

  /// Condition-match banners, "yes, we treat this" confirmation strips, selected filter chips. The recognition moment for chronic-illness visitors.
  static const Color surfaceBrandSubtle = Color(0xFFF0FAFA);

  /// Editorial feature blocks, methodology callouts, "why physician-led" panels. Pair with text/on-accent — 8.83:1.
  static const Color surfaceAccent = Color(0xFF6C3F27);

  /// Alternating warm section washes, quote blocks, patient story sections. text/primary reads 13.43:1 here.
  static const Color surfaceAccentSubtle = Color(0xFFF6EEE3);

  /// Primary buttons — "Book an appointment", "Start intake". NOT ocean/600: white on 600 measures 4.44:1 and fails body text.
  static const Color surfaceAction = Color(0xFF0A6B6D);

  /// Hover state on primary buttons. White label reads 9.22:1.
  static const Color surfaceActionHover = Color(0xFF075052);

  /// Active and pressed state on primary buttons.
  static const Color surfaceActionPressed = Color(0xFF04302F);

  /// Booking confirmed panels, "request received" states, saved-intake banners.
  static const Color surfaceSuccess = Color(0xFFEDF7E9);

  /// Out-of-state logistics notices, "call to confirm availability" strips.
  static const Color surfaceWarning = Color(0xFFFDF3E3);

  /// Form validation summaries, failed submission banners.
  static const Color surfaceError = Color(0xFFFDECEA);

  /// Cash-pay policy notes, what-to-bring callouts, first-visit and telehealth explainers.
  static const Color surfaceInfo = Color(0xFFE8F1F9);

  /// Scrim behind modals and the mobile navigation drawer. Raw rgba(20,18,16,0.60) — transparency cannot alias a primitive. Warm-tinted to match neutral/950.
  static const Color surfaceOverlay = Color(0x99141210);

  /// Illustration fills, image mattes, mascot colourways, decorative shapes behind content. The Sand brand value. SHAPE_FILL only — never text or borders (1.61:1 on surface/page).
  static const Color surfaceDecorative = Color(0xFFDFC3A4);

  /// Hover state on clickable cards and list rows — treatment cards, condition rows, FAQ headers. Shares a value with surface/disabled today but is a distinct state that will diverge. Pair with an elevation lift; hover is never the only route to content.
  static const Color surfaceHover = Color(0xFFF5F3F0);

  /// Selected state — the chosen condition card, active filter chip, current step in the intake flow. Stronger than surface/brand-subtle so selection reads as a deliberate act rather than a highlight. text/primary reads 12.8:1 here.
  static const Color surfaceSelected = Color(0xFFD6F0F1);

  /// Dividers between list items, table row rules, section separators. Decorative at 1.19:1 — exempt from 3:1 because it carries no information.
  static const Color borderSubtle = Color(0xFFE9E6E1);

  /// Card outlines, non-interactive container edges. 1.51:1 on surface/card — never the sole indicator of an interactive control.
  static const Color borderDefault = Color(0xFFD6D2CA);

  /// Input borders, checkbox and radio outlines, select field edges. 4.81:1 on surface/card, clears the 3:1 non-text requirement. Every form control uses this.
  static const Color borderStrong = Color(0xFF767266);

  /// Keyboard focus ring on every interactive element — 2px weight at 2px OFFSET. The offset is load-bearing, not decorative: it lifts the ring off the control so the ring's neighbouring colour is always the page background (6.02:1, passes WCAG 2.2 SC 1.4.11 at 3:1). Flush against a brand-filled surface it drops to 1.0:1 and disappears, because this token and surface/action both alias ocean/700. Implement as outline: 2px solid + outline-offset: 2px. Never removable, never flush.
  static const Color borderFocus = Color(0xFF0A6B6D);

  /// Secondary button outlines, selected filter chips, active tab underline, chosen condition card outline. 4.25:1 on surface/background — clears the 3:1 UI-component threshold. Pair with text/brand for the label.
  static const Color borderBrand = Color(0xFF0D8588);

  /// Left rule on editorial pull quotes, category dividers on treatment cards. 5.41:1 on surface/card.
  static const Color borderAccent = Color(0xFF8E6039);

  /// Left rule on confirmation panels, validated field outline. 3.93:1 on surface/background.
  static const Color borderSuccess = Color(0xFF3E8E2E);

  /// Left rule on advisory panels. 3.66:1 on surface/background.
  static const Color borderWarning = Color(0xFFB8730E);

  /// Failed input field outline. 5.03:1 on surface/background. Always paired with an icon and a message.
  static const Color borderError = Color(0xFFC8352A);

  /// Left rule on informational callouts. 4.94:1 on surface/background.
  static const Color borderInfo = Color(0xFF2172AC);
}
